DATAS SEGMENT 
    FARD1 DW 1,-1,100,100,100,-5,-6,-7,1,1
    FARD2 DW 1,-1,100,100,100,-5,-6,-7,1,1
DATAS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS
START:
	MOV AX,DATAS
    MOV DS,AX
    MOV ES,AX
    
    CLD
    LEA SI,FARD1
    LEA DI,FARD2
    MOV CX,10
NEXT:
    CMPSW
    JNZ STOP
    DEC CX
    JNZ NEXT
    MOV BX,00H
    JMP DONE
STOP:
    SUB SI,2
    MOV BX,SI
    MOV AL,[BX]
DONE:
    MOV AH,4CH   
    INT 21H
CODES ENDS
    END START