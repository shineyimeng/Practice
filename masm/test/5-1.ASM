DATAS SEGMENT
    ORG 2040H   
    NUM1 DB 1
    NUM2 DB 2
    LARGE DB 0
DATAS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS
START:
	MOV AX,DATAS
    MOV DS,AX
    
    MOV AL,DS:[2040H]
    MOV BL,DS:[2041H]
    CMP AL,BL
    JL NEXT
    MOV DS:[2042H],AL
    JMP EXIT
NEXT:
    MOV DS:[2042H],BL    
EXIT:    
    MOV AH,4CH   
    INT 21H
CODES ENDS
    END START