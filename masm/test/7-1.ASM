DATAS SEGMENT 
    FARD1 DB 1,-1,100,100,100,-5,-6,-7,1,1
    FARD2 DB 1,-1,100,100,100,-5,-6,-7,1,1
DATAS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS
START:
	MOV AX,DATAS
    MOV DS,AX
    MOV ES,AX
    
    CLD
    LEA SI,FARD1
    LEA DI,FARD2
    MOV CX,10
NEXT:
    CMPSB
    JNZ STOP
    DEC CX
    JNZ NEXT
    MOV BX,00H
    JMP DONE
STOP:
    DEC SI
    MOV BX,SI
    MOV AL,[BX]
DONE:
    MOV AH,4CH   
    INT 21H
CODES ENDS
    END START