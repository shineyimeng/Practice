DATAS SEGMENT
    ORG 2000H   
    SUR1 DB 1H,2H,3H,3H,5H,6H,7H,8H,9H,10H
DATAS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS
START:
	MOV AX,DATAS
    MOV DS,AX
    
    MOV SI,2000H
    MOV CX,10
    MOV AL,0
    ADD AL,0
AGAIN:  MOV BL,[SI]
    ADC AL,BL
    INC SI
    DEC CX   
    JNZ AGAIN  
    MOV [SI],AL
    
    JNC NEXT
    INC SI
    MOV AL,1
    MOV [SI],AL
NEXT:  MOV AH,4CH   
    INT 21H
CODES ENDS
    END START